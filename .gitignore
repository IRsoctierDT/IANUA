# Python
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/

# Tooling
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Local IDE / tool config
.claude/settings.local.json
.claude/agents/
.claude/launch.json
.claude/ai-command-center.code-workspace

# Secrets & local data (AGENTS.md §5)
.env
*.key
*.pem
data/
!data/.gitkeep
# data/attack/ holds the human-staged ~54 MB ATT&CK STIX bundle (never
# committed; the distilled shards under attack/data/ are the committed form)
# and data/intel/ will hold gitignored local intel snapshots. Both are
# already covered by data/ above — named here so nobody "fixes" that.

# OS junk
.DS_Store

# Generated / third-party — never tracked (locks + SBOM capture the dependency set)
node_modules/
qdrant_storage/
